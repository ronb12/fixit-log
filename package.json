{
  "name": "fixit-log",
  "version": "1.0.0",
  "private": true,
  "description": "FixIt Log production MVP",
  "scripts": {
    "check": "node --check app.js && node --check api/bootstrap.js && node --check api/records.js && node --check api/assets.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
